import numpy as np

def gauss_seidel_iteration(grid: np.ndarray):
    """
    Applies one Gauss Seidel iteration to an existing grid.

    Args:
        grid: Existing grid (2D numpy array) where every cell value represents a delta step in the
                discretised square field with side interval [0, 1].

    Returns:
        2D NumPy array containing the updated values after a GS iteration.
    """
    raise NotImplemented
